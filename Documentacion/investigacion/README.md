# Investigación documental de los 85

Salida cruda de los lotes de `Documentacion/prompt_investigacion_85.md`, un archivo por lote:
`lote-1.json` … `lote-9.json`.

**Se guarda sin editar.** Es evidencia con fuente, no canon. Las decisiones visuales que salgan
de acá se escriben en `adn_visual_personajes_v1.md` y en la matriz, y recién después bajan a
`Produccion/<id>.md`.

Un `SIN ATESTACIÓN` es un resultado válido. Habilita una decisión de diseño libre, que se
escribe etiquetada como tal. No es un hueco a rellenar con lo que el modelo suponga.
